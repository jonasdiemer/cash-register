/** @type {import('tailwindcss').Config} */
export default {
  content: ['./src/**/*.{html,js,svelte,ts}'],
  theme: {
    extend: {
      // We can add custom theme settings here later
      // colors: {
      //   primary: {...},
      //   secondary: {...}
      // }
    },
  },
  plugins: [],
}
